# Orchestrator Sandbox

This repo is an owned test fixture for the Nova framework orchestrator.

It is safe for framework-maintenance sessions to update this sandbox when testing `orchestrator.sh`.

Generated role-state files are local runtime artifacts:

- `current_role`
- `next_role`
- `checkpoint_status`
- `test_pass_count`

